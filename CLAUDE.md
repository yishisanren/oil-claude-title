# 开发约定

文档统一使用中文。修改前先明确用户的真实任务与外层项目上下文。

每次修复都要检查导致错误的规则或流程，把可复现案例加入测试或合成命名评测，并同步更新 docs/命名与回归规范.md。不要只手工修复一个标题。

标题默认省略外层已有的项目名，只有 monorepo 子项目、上级目录内的具体项目或跨项目歧义需要名称。内容模型、公司或视频主题不是可以随意删除的项目前缀。

修改标题只能追加与宿主 `/rename` 相同格式的 `custom-title` 记录行；不得改写或删除会话记录里的既有内容，不得触碰桌面端私有状态文件，不恢复原会话或向它发送消息。模型提示词只接收摘录数据。不得把真实对话、账号配置、真实会话 ID 或用户目录发布到仓库。

结构验证、模型评测、真实 Hook 触发和界面显示是四类证据，不能相互替代。模型评测需要 --live 才实际调用账号模型；普通测试不得调用外部模型。
